import type { NextPage } from 'next';
import Button from '@material-ui/core/Button';

const Home: NextPage = () => {
    return (
        <div>
            <Button>Chika Fujiwara</Button>
        </div>
    );
};

export default Home;
